jQuery(document).ready(function($){
  $(".animsition").animsition({
    inClass: 'fade-in-down',
    outClass: 'fade-out-up',
    linkElement: 'a',
    inDuration: 1000,
    outDuration: 500
  });
});
